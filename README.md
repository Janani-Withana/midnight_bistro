# Midnight Bistro

A restaurant website — menu, gallery, reservations, and admin dashboard. Built with React, Vite, and a Node/Express API.

```bash
# Frontend
npm install && npm run dev

# Backend (from /backend)
npm install && npm run dev
```

*Where the evening begins.*
